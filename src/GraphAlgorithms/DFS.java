package GraphAlgorithms;

import org.w3c.dom.Node;

public class DFS {

    //This function will search the graph starting at the given root node, using a recursive approach. The function will visit each node in the graph and print its data.
//    public void depthFirstSearch(Node root) {
//        root.visited = true;
//        System.out.println(root.data);
//        for (Node neighbor : root.neighbors) {
//            if (!neighbor.visited) {
//                depthFirstSearch(neighbor);
//            }
//        }
//    }

}
