package GraphAlgorithms;

import org.w3c.dom.Node;

import java.util.LinkedList;
import java.util.Queue;

public class BFS {

    public static void main(String[] args) {

    }

    //There are many different algorithms for searching a graph in Java. Some common ones include breadth-first search (BFS) and depth-first search (DFS).
    //This function will search the graph starting at the given root node, using a queue to keep track of the nodes to visit. The function will visit each node in the graph and print its data.
//    public void breadthFirstSearch(Node root) {
//        Queue<Node> queue = new LinkedList<>();
//        root.visited = true;
//        queue.add(root);
//        while (!queue.isEmpty()) {
//            Node current = queue.remove();
//            System.out.println(current.data);
//            for (Node neighbor : current.neighbors) {
//                if (!neighbor.visited) {
//                    neighbor.visited = true;
//                    queue.add(neighbor);
//                }
//            }
//        }
//    }

}
